In each plot, CDF of median of comment length, #comments, likes and dislikes of videos for each category is plotted.

Y axis is for CDF

X axis is normalised according to corresponding metrics, i.e. max(median of comment length), max(#comments) for videos in each category.

In folder, Like_Dislike_CDF plots for CDF of #likes for each category, #dislikes for each category and combined #likes and #dislikes are included.
